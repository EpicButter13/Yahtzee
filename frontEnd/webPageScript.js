// This is a single-line comment
let x = 5;
console.log(x);

// HTML span tags have a manipulatable property called "innerHTML"

// If new data is sent to cloud variable that stores the roll numbers, fetch it and show it to the player
let rollString = "5 2 3 1 8";

// Establish conditionals to change the data held in "rollString"
            // INSERT CODE HERE //

// Assign the new value to the HTML element that shall display that data to the player
document.getElementById("current_roll").innerHTML = rollString;
